server.port=${SERVER_PORT:8080}
spring.application.name=bff-service

# URL del orq-service — configurable via variable de entorno ORQ_SERVICE_URL
orq.service.url=${ORQ_SERVICE_URL:http://orq-service:8080}
